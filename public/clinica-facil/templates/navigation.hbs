{{#if currentUser}}
    <nav class="navbar-default navbar-static-side" role="navigation">
        <div class="sidebar-collapse">

            <!-- Close canvas menu used only on OffCanvas page -->
            <a class="close-canvas-menu"><i class="fa fa-times"></i></a>
            <!-- End offCanvas menu toggle -->

            <ul class="nav" id="side-menu">
                <li class="nav-header">
                    <div class="dropdown profile-element">
                        <span>
                            <img alt="profile image" id="mini-profile-img" class="img-circle" src="images/default-user-image.png" data-pin-nopin="true" style="width:50px">
                        </span>
                        <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                            <span class="clear">
                                <span class="block m-t-xs">
                                    <strong class="font-bold">{{currentUser.profile.firstName}}</strong>
                                </span>
                                <span class="text-muted text-xs block">
                                    {{_ currentUser.profile.group}} <b class="caret"></b>
                                </span>
                            </span>
                        </a>
                        <ul class="dropdown-menu animated fadeInRight m-t-xs">
                            <li><a href="{{pathFor route='users'}}"><i class="fa fa-user" aria-hidden="true"></i> {{_ 'common_profile'}}</a></li>
                            <li class="divider"></li>
                            <li><a class="logout" href="{{pathFor 'logout'}}"><i class="fa fa-sign-out" aria-hidden="true"></i> {{_ 'common_logout'}}</a></li>
                        </ul>
                    </div>
                    <div class="logo-element">
                        CF
                    </div>
                </li>

                <li data-nav="dashboard" class="{{isActivePath regex='^dashboard$'}}">
                    <a href="{{pathFor route='dashboard'}}" class="hide-on-phone"><i class="fa fa-dashboard"></i> <span class="nav-label">{{_ 'common_dashboard'}}</span> </a>
                </li>
                {{#if isInRole 'super-admin,administration,recepcionist'}}
                    <li data-nav="schedule" class="{{isActivePath regex='^schedule$'}}">
                        <a href="{{pathFor route='schedule'}}" class="hide-on-phone"><i class="fa fa-calendar" aria-hidden="true"></i> <span class="nav-label">{{_ 'schedule_schedule'}}</span></a>
                    </li>
                {{/if}}
                {{#if isInRole 'super-admin,administration,recepcionist,medical_doctor,nurse'}}
                    <li data-nav="patients" class="{{isActivePath regex='^patients(/|$)'}}">
                        <a href="{{pathFor route='patientCreate'}}">
                            <span class="patient-gender-icon"><i class="fa fa-male"></i><i class="fa fa-female"></i></span>
                            <span class="nav-label">{{_ 'common_patients'}}</span>
                            <span class="fa arrow"></span>
                        </a>
                        <ul class="nav nav-second-level collapse {{isActivePath regex='^patients(/|$)' className='in'}}">
                            {{#if isInRole 'super-admin,administration,recepcionist,medical_doctor,nurse'}}
                                <li data-nav="patients-create" class="{{isActivePath regex='^patients/create$'}}">
                                    <a href="{{pathFor route='patientCreate'}}" class="hide-on-phone"><i class="fa fa-user-plus" aria-hidden="true"></i> <span class="nav-label">{{_ 'patients_newpatient'}}</span></a>
                                </li>
                            {{/if}}
                             {{#if isInRole 'super-admin,administration,recepcionist,nurse'}}
                                <li data-nav="patients-list" class="{{isActivePath regex='^patients$'}}">
                                    <a href="{{pathFor route='patientList'}}" class="hide-on-phone"><i class="fa fa-list-ul" aria-hidden="true"></i> <span class="nav-label">{{_ 'patients_list'}}</span></a>
                                </li>
                            {{/if}}
                        </ul>
                    </li>
                {{/if}}
                {{#if isInRole 'super-admin,administration'}}
                    <li data-nav="doctors" class="{{isActivePath regex='^doctors(/|$)'}}">
                        <a href="{{pathFor route='doctorList'}}" class="hide-on-phone"><i class="fa fa-user-md"></i> <span class="nav-label">{{_ 'users_doctors'}}</span></a>
                    </li>
                {{/if}}
                {{#if isInRole 'super-admin,administration,medical_doctor'}}
                    <li data-nav="icd10" class="{{isActivePath regex='^icd10$'}}">
                        <a href="{{pathFor route='icd10List'}}" class="hide-on-phone"><i class="fa fa-bug" aria-hidden="true"></i> <span class="nav-label">{{_ 'common_icd'}}</span></a>
                    </li>
                {{/if}}
                {{#if isInRole 'super-admin,administration,medical_doctor'}}
                    <li data-nav="drugs" class="{{isActivePath regex='^drugs(/|$)'}}">
                        <a href="{{pathFor route='drugList'}}" class="hide-on-phone"><img style="width: 16px; margin-right: 4px;" src="images/drugs.png" /> <span class="nav-label">{{_ 'drugs_drugs'}}</span></a>
                    </li>
                {{/if}}
                {{#if isInRole 'super-admin,administration'}}
                    <li data-nav="reports" class="{{isActivePath regex='^reports/'}}">
                        <a href="{{pathFor route='reportAppointments'}}">
                            <i class="fa fa-newspaper-o" aria-hidden="true"></i>
                            <span class="nav-label">{{_ 'reports_reports'}}</span>
                            <span class="fa arrow"></span>
                        </a>
                        <ul class="nav nav-second-level collapse {{isActivePath regex='^reports/' className='in'}}">
                            <li data-nav="reports-appointments" class="{{isActivePath regex='^reports/appointments$'}}">
                                <a href="{{pathFor route='reportAppointments'}}" class="hide-on-phone"><i class="fa fa-file-text-o" aria-hidden="true"></i> <span class="nav-label">{{_ 'reports_appointments'}}</span></a>
                            </li>
                            <li data-nav="reports-patients" class="{{isActivePath regex='^reports/patients$'}}">
                                <a href="{{pathFor route='reportPatients'}}" class="hide-on-phone"><i class="fa fa-users" aria-hidden="true"></i> <span class="nav-label">{{_ 'reports_patients'}}</span></a>
                            </li>
                            <li data-nav="reports-production" class="{{isActivePath regex='^reports/production$'}}">
                                <a href="{{pathFor route='reportProduction'}}" class="hide-on-phone"><i class="fa fa-line-chart" aria-hidden="true"></i> <span class="nav-label">{{_ 'reports_production'}}</span></a>
                            </li>
                        </ul>
                    </li>
                {{/if}}
                {{#if isInRole 'super-admin,administration,medical_doctor'}}
	                <li data-nav="settings" class="{{isActivePath regex='^settings$|^specialties(/|$)|^exam-catalog(/|$)|^document-models(/|$)|^form-models(/|$)|^users$|^import$'}}">
	                    <a href="{{pathFor route='settingsForm'}}">
	                        <i class="fa fa-cogs" aria-hidden="true"></i>
	                        <span class="nav-label">{{_ 'settings_settings'}}</span>
	                        <span class="fa arrow"></span>
	                    </a>
	                    <ul class="nav nav-second-level collapse {{isActivePath regex='^settings$|^specialties(/|$)|^exam-catalog(/|$)|^document-models(/|$)|^form-models(/|$)|^users$|^import$' className='in'}}">
                            {{#if isInRole 'super-admin'}}
                                <li data-nav="settings-general" class="{{isActivePath regex='^settings$'}}">
                                    <a href="{{pathFor route='settingsForm'}}" class="hide-on-phone"><i class="fa fa-cog" aria-hidden="true"></i> <span class="nav-label">{{_ 'settings_general'}}</span></a>
                                </li>
                            {{/if}}
                            {{#if isInRole 'super-admin,administration,medical_doctor'}}
	                       <li data-nav="specialties" class="{{isActivePath regex='^specialties(/|$)'}}">
		                        <a href="{{pathFor route='specialtyList'}}" class="hide-on-phone"><i class="fa fa-stethoscope"></i> <span class="nav-label">{{_ 'specialties_specialties'}}</span></a>
		                    </li>
                            {{/if}}
                            {{#if isInRole 'super-admin,administration,medical_doctor'}}
	                       <li data-nav="exam-catalog" class="{{isActivePath regex='^exam-catalog(/|$)'}}">
		                        <a href="{{pathFor route='examCatalogList'}}" class="hide-on-phone"><i class="fa fa-flask"></i> <span class="nav-label">{{_ 'exam-catalog_exam-catalog'}}</span></a>
		                    </li>
                            {{/if}}
                            {{#if isInRole 'super-admin,administration,medical_doctor'}}
                                <li data-nav="document-models" class="{{isActivePath regex='^document-models(/|$)'}}">
                                    <a href="{{pathFor route='documentModelList'}}" class="hide-on-phone"><i class="fa fa-file-text" aria-hidden="true"></i> <span class="nav-label">{{_ 'document-models_document-models'}}</span></a>
                                </li>
                            {{/if}}
                            {{#if isInRole 'super-admin,administration,medical_doctor'}}
                                <li data-nav="form-models" class="{{isActivePath regex='^form-models(/|$)'}}">
                                    <a href="{{pathFor route='formModelsList'}}" class="hide-on-phone"><i class="fa fa-id-card" aria-hidden="true"></i> <span class="nav-label">{{_ 'form-models_form-models'}}</span></a>
                                </li>
                            {{/if}}
                            {{#if isInRole 'super-admin'}}
		                    <li data-nav="users" class="{{isActivePath regex='^users$'}}">
		                        <a href="{{pathFor route='users'}}" class="hide-on-phone"><i class="fa fa-users"></i> <span class="nav-label">{{_ 'users_users'}}</span></a>
		                    </li>
                            {{/if}}
                            {{#if isInRole 'super-admin'}}
		                    <li data-nav="import" class="{{isActivePath regex='^import$'}}">
		                        <a href="{{pathFor route='import'}}" class="hide-on-phone"><i class="fa fa-database" aria-hidden="true"></i> <span class="nav-label">{{_ 'import_data-import'}}</span></a>
		                    </li>
                            {{/if}}
	                    </ul>
	                </li>
                {{/if}}
            </ul>
        </div>
    </nav>
{{/if}}
