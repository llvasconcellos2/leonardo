Configuring HTTPS on Wildfly
This configuration is only meant to enable HTTPS on a development machine running the Wildfly Application Server. I didn't go deep into the security side since I'm not the one who administers the serv
@@@
This configuration is only meant to enable HTTPS on a development machine running the Wildfly Application Server. I didn't go deep into the security side since I'm not the one who administers the server where the application will run. So don't use this in a production environment.

I'm using Wildfly 8.2.0.Final.

1.  The first thing to do is to enter the Wildfly installation directory, into the _**./standalone/configuration/**_ folder, and run the following command:

    [[CODE]]

2.  Edit the _**./standalone/configuration/standalone.xml**_ file and add the following XML inside _**security-realms**_:

    [[CODE]]

3.  Next, add the line below inside subsystem undertow -> server

    [[CODE]]

    Here's how the complete entry looks:

    [[CODE]]

Done — restart the server and go to [https://localhost:8443](https://localhost:8443/).
